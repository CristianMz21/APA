"""Semantic document import system.

Provides the ``SemanticImporter`` pipeline for converting raw DOCX files
into rich ``SemanticDocument`` objects using Builder + Chain of Responsibility
patterns.
"""
